name: flutter_ui_agent
description: Dynamic UI rendering from JSON schemas generated by AI

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # For JSON handling (built-in)
  # dart:convert

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - ui_schema.json
